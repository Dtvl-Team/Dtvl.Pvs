@model PvOption<DtvlPvs>

@{
    Model
    .UseGlobalSlots()
    .AddParentAttr(new
    {
        @class = "bg-danger",
    });
}

@if (Model.TryGetSlotRender(CollapseSlot.Content, out var ContentRender))
{
    @ContentRender
}
